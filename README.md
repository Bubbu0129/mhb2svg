```
$ python main.py --help
usage: main.py [-h] [-c] (-f FILE | -l LINK) [-p PADDING] [-r RATIO]

options:
  -h, --help            show this help message and exit
  -c, --color           Enable color (default black & white)
  -f, --file FILE       Path to .mhb file
  -l, --link LINK       MAXHUB URL containing the s_id argument
  -p, --padding PADDING Padding size for .svg (integer)
  -r, --ratio RATIO     Stoke width ratio (float)
```
